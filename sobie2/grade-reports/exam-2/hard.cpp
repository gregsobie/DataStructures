#include "linkedlist.h"

void intersection(LinkedList* lst1,
           LinkedList* lst2,
           LinkedList &intersect)
{
	/*
	if(lst1->head == NULL || lst2->head = NULL)
		return;
	else {
		Node* lst1temp = lst1->head;
		Node* lst2temp = lst2->head;

		if(lst1temp == lst2temp){
			
		}
	*/
}
